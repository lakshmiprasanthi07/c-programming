#include <stdio.h>
int main() {
    int num;
    scanf("%d",&num);
    if(num>=10 && num<=20)
    {
        printf("number is within range");
    }
else
printf("number is outside in range");
    return 0;
}